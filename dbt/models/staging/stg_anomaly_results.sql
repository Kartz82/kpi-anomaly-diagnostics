select
    cast(date as date) as date,
    cast(kpi_name as text) as kpi_name,
    cast(region as text) as region,
    cast(device_type as text) as device_type,
    cast(browser as text) as browser,
    cast(channel as text) as channel,
    cast(business_segment as text) as business_segment,
    cast(actual_value as double precision) as actual_value,
    cast(expected_value as double precision) as expected_value,
    cast(moving_baseline as double precision) as moving_baseline,
    cast(percent_variance as double precision) as percent_variance,
    cast(monitoring_status as text) as monitoring_status,
    cast(threshold_breached as boolean) as threshold_breached,
    cast(z_score as double precision) as z_score,
    cast(zscore_anomaly as boolean) as zscore_anomaly,
    cast(zscore_severity as text) as zscore_severity,
    cast(predicted_value as double precision) as predicted_value,
    cast(residual as double precision) as residual,
    cast(residual_percent as double precision) as residual_percent,
    cast(residual_anomaly as boolean) as residual_anomaly,
    cast(residual_severity as text) as residual_severity,
    cast(combined_anomaly as boolean) as combined_anomaly,
    cast(combined_severity as text) as combined_severity,
    cast(is_incident as boolean) as is_incident,
    cast(incident_id as text) as incident_id,
    cast(incident_type as text) as incident_type,
    cast(incident_description as text) as incident_description
from {{ source('raw', 'raw_anomaly_results') }}

